#count occurrences of a substring in a string
init_string = "constantinople"
init_substring = "o"
init_substring2 = "ant"
print(init_string.count(init_substring))
print(init_string.count(init_substring2))
